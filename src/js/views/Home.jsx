import React from "react";

const Home = () => {
  const miniStyles = {
    fontSize: "30px",
  };

  return (
    <div>
      <h1 className={miniStyles}>Nothing over here...</h1>
    </div>
  );
};

export default Home;
